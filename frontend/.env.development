# 本地开发环境变量：前端(5173)统一走相对路径 /api，由 vite.config.js 的 proxy 转发到本地后端。
# 后端地址改在 vite.config.js 的 server.proxy.target 维护（当前本地后端在 8081）。
# 生产构建不带本文件，baseURL 回落源码默认 '/api'（相对路径，由 Nginx 同源反代）。
# VITE_API_BASE=http://localhost:8080/api
