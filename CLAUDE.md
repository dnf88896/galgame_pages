# Galgame 论坛项目（galgame_pages）

技术栈：**Vue3 + Element Plus**（`frontend/`，dev server 5173）/ **Spring Boot 4.1 + JDBC**（`backend/`，jar 8080）/ **MySQL 8.4**（库名 `galgame`）。后端 schema 由 `schema.sql` 启动时自动执行（`CREATE TABLE IF NOT EXISTS` 幂等），共 15 张表。前端工具与 MySQL 都装在 `C:\Users\yinsh\dev-tools`。


## 启动与重启
- **MySQL**：数据目录在**项目内** `database/mysql-data`，配置 `database/my.ini`（basedir 指向 `C:\Users\yinsh\dev-tools\mysql`），用 `mysqld --defaults-file=...\database\my.ini` 后台启动（本机非管理员，无服务）。完整启动步骤见 `README.md`。
- **后端重启流程**：`netstat -ano | grep ':8080' | grep -i listen` 拿 PID → `taskkill //F //T //PID <pid>` → 在 `backend/` 跑 `mvn -q -DskipTests package` → 后台 `java -jar target/galgame-backend-0.0.1-SNAPSHOT.jar > app.log 2>&1` → `curl http://localhost:8080/api/boards` 轮询到 200。
- **前端**：`npm run dev`（5173），Vite HMR 实时生效。

## 后端关键约定
- ⚠️ **JSON 依赖是 Jackson 3**（`tools.jackson.*` 包名，非旧版 `com.fasterxml.jackson.*`）：手写 Jackson 代码/导入必须用 `tools.jackson.databind.json.JsonMapper`、`tools.jackson.core.type.TypeReference`；Jackson 3 读写方法抛**运行时异常**，不抛受检 `IOException`（catch `IOException` 会编译报错）。曾因此让子代理按 Jackson 2 写导入导致编译失败。
- `spring.jackson.property-naming-strategy=SNAKE_CASE` 自动处理 record → snake_case JSON。
- 表结构：新表 `galgames`/`galgame_tags`（Galgame 作品库）由 schema.sql 每次启动自动建（IF NOT EXISTS 幂等），**无需改 DatabaseMigrator**。

## Galgame 作品库（已完成完整 CRUD）
- 接口：`GET /api/galgames`（列表 + q 名称搜索 + tags 多标签 **AND** 过滤，**只返回 approved**）、`GET /{id}`（详情）、`POST`（创建，登录用户，管理员→approved / 普通用户→pending）、`PUT /{id}`（编辑，`updated_at` 由 `ON UPDATE CURRENT_TIMESTAMP` 自动刷新；管理员可改一切，创建者仅 status≠approved 可改自己）、`DELETE /{id}`（管理员可删一切，创建者仅 pending 可删自己）、`POST /image`（封面上传，登录用户）。权限：401 未登录 / 403 无权 / 404（他人看非 approved）。
- 前端：`/galgame` 列表卡点击进 `/galgame/:id` 详情页（`GalgameDetailView.vue`），管理员在详情页**内联编辑**（表单复用 AddGalgameView 字段：名称/4 组标签/封面上传/简介/制作人员/资源链接）+ **删除**（`ElMessageBox.confirm` 确认）。

## Galgame 提交审核（已完成本地，2026-08-28，待部署）
- **流程**：普通登录用户在 `/galgame/new` 提交 galgame 信息 → 状态 `pending` 待审核；管理员在 Galgame 页顶栏「审核galgame信息」（`/galgame/review`）看待审列表 → 点进详情「通过审核」/「拒绝」（拒绝必填理由）。通过后公开上架；拒绝后提交者详情页可见 `reject_reason`。
- **数据**：`galgames` 加 3 列——`status VARCHAR(20) NOT NULL DEFAULT 'approved'`（存量数据即已上架）、`reject_reason VARCHAR(500) NULL`、`reviewed_at DATETIME NULL`；`schema.sql` + `DatabaseMigrator`（3 行 `ensureColumn`）双写。
- **后端**：`Galgame` model 加 `status/rejectReason/reviewedAt/creator`（creator 是 BASE_COLUMNS 子查询 `(SELECT COALESCE(nickname, username) FROM users u WHERE u.id = g.created_by)`，不 JOIN 避免影响行数）；`GalgameDao` 加 `findPending()`（status='pending' ORDER BY created_at DESC）+ `updateStatus(id, status, rejectReason)`（`SET status=?, reject_reason=?, reviewed_at=NOW()`）；`update()` 不碰 status 列。接口：`GET /api/galgames/pending`（管理员，含 creator）、`POST /api/galgames/{id}/review`（管理员，body `{status, reason?}`，rejected 无 reason → 400「请填写拒绝理由。」，reason≤500）；评分 `POST /{id}/rating` 仅 approved（pending/rejected → 400「该条目尚未通过审核，不能评分。」）；`GET /{id}` pending/rejected 仅创建者/管理员可见**且不** incrementView，否则 404。`/pending` 精确路径优先于 `/{id}` 模板不冲突。
- **前端**：路由加 `/galgame/review`（在 `:id` 前）；`GalgameView` 顶栏右侧 `gal-header-actions`（审核按钮 warning 色在左、添加按钮 `v-if="isLoggedIn"` 在右）；`AddGalgameView` 挡板改 `!loggedIn`（去登录带 redirect）、标题「提交 Galgame 信息」、成功文案按角色区分；`GalgameDetailView` 加状态 tag（pending→warning 待审核 / rejected→danger 已拒绝+理由 el-alert）、`canEdit`/`canDelete` computed（`isAdmin || (isCreator && status!=='approved')`）、管理员+pending 时「通过/拒绝」按钮、评分区仅 approved、meta 显示「提交人」；`GalgameReviewView.vue` 新建待审列表（非管理员挡板 + 空态「暂无待审核的 Galgame 提交」）。
- **「我的提交」**（2026-08-28 补，同批部署）：普通用户提交后公开列表看不到自己的 pending 条目，需顶栏「我的提交」按钮（`v-if="isLoggedIn"`，位于审核与添加按钮之间）进 `/galgame/mine` 页查看自己全部提交（含各状态标签 + 拒绝理由），点进详情即可编辑/删除。接口 `GET /api/galgames/mine`（登录，`findByCreator`：`WHERE created_by=? ORDER BY created_at DESC, id DESC` 兜底同秒）。
- 冒烟：`D:\claude code\review_smoke.py`（自建管理员 galgame1 认证 + 普通用户 A/B，20 项断言全 PASS：approve/reject/权限/编辑删除/评分限制/理由可见）；`D:\claude code\mine_smoke.py`（mine 接口 10 项全 PASS：未登录 401 / 全状态+reject_reason+creator / id 倒序 / 公开列表隔离）。

## 三个小功能：搜索文案 / 审核通过 +10 萌点 / +n 萌点屏幕提示（已完成本地与线上，2026-08-28）
- **搜索文案**：`SearchUserView.vue` 输入框 placeholder「输入用户名搜索…」→「输入用户名或昵称搜索…」（与 `UserDao.findByNameLike` 同时搜账号名+昵称的行为一致）。
- **审核通过给提交者 +10 萌点（防刷分）**：新增 `galgames.moe_awarded TINYINT(1) NOT NULL DEFAULT 0`（schema.sql + DatabaseMigrator `ensureColumn`），`GalgameDao.claimMoeAward(id)` 执行 `UPDATE galgames SET moe_awarded=1 WHERE id=? AND moe_awarded=0`——**原子置位，仅首次成功返回 true**（防并发/反复审核重复发放）。加分路径两处：
  - `doCreate`：管理员创建**即上架**（status=approved）→ 尝试 claimMoeAward → 成功则 `userDao.adjustMoePoints(userId, 10)`（管理员自己是提交者，同样 +10）。
  - `review`：审核通过（approved）→ 尝试 claimMoeAward → 成功则给 `saved.createdBy()` +10；响应 `Map` 追加 `moe_granted`——**仅当操作者==提交者才返回 10**，否则 0（审核别人时不给管理员自己弹提示）。rejected→approved 反复切换也不会重复加分（moe_awarded 已置 1）。
  - ⚠️ 这是项目第一条「**按条目只奖一次**」的原子防重逻辑（区别于签到/发帖的「每日一次」INSERT IGNORE），对应冒烟断言 `re-approve no double moe`、`reject -> no moe`、`admin own re-approve no double moe & moe_granted=0`。
- **+n 萌点屏幕提示（通用检测萌点变化，2026-08-28 重构）**（纯前端）：`frontend/src/utils/moeGain.js` 统一入口 `refreshMoe(knownMoe?)`——调用方已持有最新值（如 App.refreshUser 拉的 /auth/me）直接传，否则拉 `GET /auth/me`；与 localStorage 基线（`galforum_moe_baseline_<uid>`，**按用户 id 隔离防串号**）比较，**增量>0 就弹「+n萌点」**，并同步基线 + user store。首次无基线只建立不弹（避免把历史萌点误报为新增）。`MoeToast.vue` 订阅 `moeGain` ref 渲染 `.moe-toast`（fixed 顶 45% 居中，color #ff7d5c，font-size 30px，z-index 9999，pointer-events none，`moe-float` 动画 2s：scale-in → 停留 → translateY 上浮淡出，2s 后 setTimeout 隐藏），在 `App.vue` SettingsPanel 后挂载。
  - **触发点（所有加分源统一调 refreshMoe）**：`App.vue refreshUser`（登录态恢复建立基线）、`ProfileView` 签到 +10、`ComposeView` 发帖（每日首次 +10）、`PostView` 评论（每日首次 +5）、`GalgameDetailView` 审核通过（提交者=操作者时 +10，审核他人无变化不弹）、`AddGalgameView` 管理员创建即上架 +10、`GalgameMineView` 进入时（覆盖被动加分如被管理员审核通过）。
  - 非首次发帖/评论（每日奖励已领）时 delta=0 不弹，天然正确；萌点减少不弹。旧 showMoeGain/getMoeBaseline/setMoeBaseline 导出已移除，只剩 `showMoeGain` 供 refreshMoe 内部用。
  - 算法验证：`/tmp/moe_detect_test.js`（node 模拟基线检测 9 场景：首次建基线/签到/重复签到/发帖/评论/审核通过/减少不弹/多用户隔离）9/9 PASS。
- 冒烟：`D:\claude code\moe_smoke.py`（10 项全 PASS：初始 0、pending 不加分、approve 提交者+10、moe_granted=0（审核人≠提交人）、re-approve 不重复、reject 不加、管理员创建即上架+10、管理员自我 re-approve 不重复且 moe_granted=0）。⚠️ 服务器部署冒烟（部署时写）若从创建响应提取 id，`user` 对象里也有 `id`——必须先截 `"galgame":{...}` 段（同投票选项坑）。

## 首页筛选排序（已完成）
- 首页搜索框左侧「排序」按钮，4 种排序：`GET /api/posts?sort=`，取值 `time`（默认，时间倒序）/ `hot`（热度 = view + reply×3 + like×5 倒序）/ `likes`（点赞量倒序）/ `following`（只看我关注的人的帖子）。
- 后端：`PostFilter` record 加 `sort` 字段（`of(q, category, section, sections, sort)`）；`PostDao.findAll(filter, hidden, currentUserId)` 新增排序分支 + following 过滤（未登录或未关注时返回空列表，SQL 用 `user_id IN (SELECT following_id FROM follows WHERE follower_id = ?)` 子查询）；`PostController.list` 加 `@RequestParam sort` 并透传当前 uid。
- 前端：`HomeView.vue` 顶栏 `el-dropdown`「排序」按钮（含 `filter-arrow`/`filter-check` 样式），`sortBy` ref 默认 `time`，切换即 `load()`；未登录点「关注的人」提示先登录；following 空态文案「你关注的人还没有发帖。」。

## Galgame 页面排序（已完成，本地与线上 2026-08-28 已同步部署）
- 排序行在筛选区顶部：总浏览数 / 创建顺序 / 发售日期 / 评分；`GET /api/galgames?sort=`，取值 `created`（默认，管理员添加顺序倒序）/ `views`（浏览数倒序）/ `views_asc`（浏览数升序）/ `rating`（评分从高到低）/ `rating_asc`（评分从低到高）/ `release_date_desc`（发售日期从新到旧）/ `release_date_asc`（从旧到新）。
- **三键两态**：总浏览数 / 评分 / 发售日期 三个排序按钮都是两态切换——第一下倒序（从高到低/从新到旧，文字右侧紧贴 **↑**），再按一下升序（从低到高/从旧到新，箭头变 **↓**）；两态都算激活高亮；「创建顺序」单选无箭头。前端统一用 `SORT_DIR = { views: {desc:'views',asc:'views_asc'}, rating: {...}, release_date: {desc:'release_date_desc',asc:'release_date_asc'} }` 驱动 `setSort`/`isSortActive`/`sortArrowOf`，`load()` 传 `sort`。
- ⚠️ NULL 排最后：`GalgameDao.orderBy` 用 `(col IS NULL)` 做 ORDER BY 首键（false=0 在 true=1 前），未评分/无发售日期的条目恒排最后；rating 同分再按 `rating_count` 降序（asc 升序）；`view_count` 非空默认 0 无需 NULL 处理。
- 浏览数：`galgames.view_count`（DatabaseMigrator `ensureColumn` 迁移，schema.sql 已加默认 0），`GET /api/galgames/{id}` 详情访问 +1 并重查返回最新值；卡片显示「浏览 N」。
- 列表卡片 meta 区（`.gal-views`，flex 两段）显示「浏览 N」+「评分 X.X / 10」（无评分显示「暂无评分」）；列表接口返回 `rating_avg`/`rating_count`（同一 mapRow）。⚠️ 字段名必须是 `rating_avg`（旧 `rating` 已改名，用 `p.rating` 会恒为「暂无评分」）。
- 后端：`Galgame` model 加 `viewCount`；`GalgameDao` BASE_COLUMNS 加 `g.view_count`、`findAll(q, tags, sort)` + `orderBy` helper（含 views/views_asc/rating/rating_asc/release_date_desc/release_date_asc 分支）、`incrementView(id)`；`GalgameController.list` 加 `sort` 参数、`getById` 访问计数。
- 前端：`GalgameView.vue` 排序行 `gal-filter-btn` 复用筛选按钮样式，`sortBy` 默认 `created`；`.gal-sort-arrow` 样式 margin-left:4px。

## Galgame 列表搜索：作品名 / 会社（已完成本地与线上，2026-08-28）
- 前端 `GalgameView.vue` 标题下新增搜索区：`el-select`（**作品名** / **会社**）+ `el-input`（回车 / 按钮 / 清空触发），关键词与现有筛选（类型/语言/平台/作品）、排序可叠加；切字段时若有关键词自动重搜。
- 后端 `GET /api/galgames` 新增 `field` 参数（`name` 默认 / `staff`）：`GalgameDao.findAll(q, tags, sort, field)` 在 q 非空时按 `"staff".equalsIgnoreCase(field)` 分支拼接 `g.staff LIKE` 或 `g.name LIKE`（白名单判断，不把用户输入拼进列名，防注入）。
- **会社信息无独立字段，存于 `staff`（制作人员）字段**——用户确认的存储位置；历史数据 staff 即为会社/制作人员。
- 冒烟：`D:\claude code\search_smoke.py`（9 项全 PASS：无 q 全量 / 名称命中+miss / 会社命中+miss / 非法 field 回退名称 / 大小写不敏感 / 空词等价无搜索）。⚠️ 中文关键词（会社名）拼 URL 必须 `urllib.parse.quote`，否则 urllib 发 ASCII 报 UnicodeEncodeError。
- 部署脚本：`D:\claude code\deploy_galgame_search_to_server.py`（jar+dist → 重启 8081 → 服务器搜索冒烟 6 项 → 清理 → assets 校验），部署时 6/6 PASS。

## Galgame 详情页发售日期 + 评分（已完成）
- **发售日期**（`galgames.release_date DATE NULL`，管理员可创建/编辑）：详情页与新建页都有 el-date-picker（`value-format="YYYY-MM-DD"`，中文 locale 在 `main.js` 用 `app.use(ElementPlus, { locale: zhCn })`）。
- **评分**（`galgames.rating_avg DECIMAL(4,2)` + `rating_count INT NOT NULL DEFAULT 0`，**管理员不可写**）：任何登录用户可给 galgame 打 0~10 分（满分 10，el-rate 可半星），**一人一票**不能重复评。
  - 存储模型：只存「上一个平均分 + 总人数」，新评分增量更新 `new_avg = (old_avg * old_count + score) / (count+1)`（首次直接 = score）；DB 存精确两位小数，显示最多一位。
  - 防重：`galgame_ratings(galgame_id, user_id)` 主键表一人一票，`rate()` 捕获 `DuplicateKeyException` → 409「你已经评过分了。」；表含 `score DECIMAL(3,1)` 存该用户评分（详情回显用）。⚠️ **旧记录（加列前）score 为 NULL 无法回补，重新进详情仍显示 0 星**；新评分即刻回显。
  - 接口：`POST /api/galgames/{id}/rating` body `{score}`（401 未登录 / 404 不存在 / 400 越界或非数字）；`GET /{id}` 带登录态返回 `rated`（是否已评，禁用评分）+ `my_score`（当前用户自己的分，未评/旧记录为 null）→ 前端据此回显。
  - 前端：`GalgameDetailView.vue` 展示「评分 X.X / 10（N 人评分）」+ el-rate（max=10 allow-half）+「提交评分」按钮；`loadDetail` 回填 `myScore = data.my_score ?? 0`，已评过禁用并显示自己的分（修复：以前只返回 rated 布尔、无 my_score，重新进详情恒 0 星）。
- ⚠️ **SNAKE_CASE 契约坑**：后端 `spring.jackson.property-naming-strategy=SNAKE_CASE` 下，`releaseDate` 这类多词 record 字段对应 JSON 键是 `release_date`。前端 payload 必须发 `release_date`（`releaseDate` 会被 Jackson 忽略 → DB 写 NULL）。单字段名（name/tags/links 等）camelCase=snake_case 不受影响。
- 迁移：`schema.sql` galgames 加 `release_date`/`rating_avg`/`rating_count`，`galgame_ratings` 加 `score DECIMAL(3,1)`；`DatabaseMigrator.migrateGalgameRating()` 把旧 `rating` 列 `CHANGE COLUMN` 成 `rating_avg`（新库直接建），兜底建 `galgame_ratings` + `ensureColumn` score。
- 后端：`Galgame` model 字段 `... viewCount, releaseDate, ratingAvg, ratingCount`；`GalgameDao` BASE_COLUMNS 含 `g.rating_avg, g.rating_count`、`mapRow` 读两列、`rate()` 插分（galgame_ratings 带 score）+ 增量更新 avg/count、`findScoreByUser()`（无记录→empty；旧记录 score NULL→0.0 保持「已评分」态）；`GalgameController` 注入 JsonMapper、`getById` 返回 Galgame 转 Map 加 `rated`+`my_score`、新增评分接口、创建/编辑校验 releaseDate 格式（非法 400「发售日期格式应为 YYYY-MM-DD。」，可空）。
- 前端：`GalgameDetailView.vue` 展示「发售：YYYY-MM-DD」，编辑模式与 `AddGalgameView.vue` 都加了 date-picker（enterEdit 回填 `d.release_date`）。
- 列表页「发售日期/评分/总浏览数」排序**已启用**（见「Galgame 页面排序」一节），冒烟脚本 `D:\claude code\deploy_galgame_sort_to_server.py`（上传 jar+dist → 重启 8081 → 服务器插测试数据断言六种排序顺序 → 清理 → nginx assets 校验）。⚠️ 服务器部署重启后端三个坑（都记过代价）：
  1. **kill 与 start 必须分开两个 exec_command**——同一命令里 `ps -ef | grep '[g]algame'` 会匹配到当前 bash -c 自身（命令行含 galgame jar 名），kill 自杀导致 start 不执行（app.log 无新启动记录）。
  2. **start 命令必须带 `</dev/null & sleep 1; echo started`**——否则 paramiko channel 关闭会把新起的 java 一并带走（报「backend NOT up」但进程实际可能存活，先查 `ps -ef | grep '[j]ava'` + app.log）。
  3. **Python 字符串里 curl `-w '%{http_code}'` 用单 `%`，不能用 `%%`**——普通字符串 `%%` 就是两个字面 `%`，传给远程 curl 会输出字面 `%200`，永远匹配不上 `"200"`（诊断时输出 `code=[200]` 说明后端其实健康）。

## Galgame 详情页评论区（已完成本地并部署线上，2026-08-29）
- **功能与帖子评论区一致（除置顶）**：发评论 / 嵌套回复（`parent_id` + `parent_author` 快照，父删仍显示「回复 @xx」）/ 点赞 / 点踩（独立不互斥）/ 删除（仅作者本人）/ 举报（管理员处理可删）/ emoji / @输入框；**无置顶、无评论编辑、无分页**（评论随详情全量返回）。
- **表**：`galgame_replies`（无 `is_pinned`；`author`/`parent_author` 作者名快照；FK：galgame_id→galgames CASCADE、user_id→users SET NULL、parent_id→galgame_replies SET NULL）、`galgame_reply_likes`/`galgame_reply_dislikes`（复合主键 + 级联）。DatabaseMigrator `ensureGalgameReplyTables()` 兜底建表。
- **接口**（全部**手动 `tokenService.resolveUserId` 鉴权**，未注册拦截器，与 `/api/galgames` 一致）：
  - `GET /api/galgames/{id}`（改造）：响应新增 `replies[]`（`id/galgame_id/user_id/parent_id/parent_author/author/content/created_at/like_count/dislike_count/liked/disliked`），登录时批量注入 liked/disliked（`findLikedReplyIds` 防 N+1），未登录省略；排序 `created_at ASC, id ASC`
  - `POST /api/galgames/{id}/replies`：body `{content, parent_id?}`；仅 approved 可评论（400「该条目尚未通过审核，不能评论。」）；content 空 400 / >2000 400 / 父回复不存在或跨条目 400「父回复不存在。」；成功 +5 萌点（`claimDailyReward(userId, "galgame_reply", 5)` 尽力而为）→ 201 评论对象
  - `POST /api/galgame-replies/{id}/like`→`{liked, like_count}`、`/dislike`→`{disliked, dislike_count}`（toggle，`GREATEST(0,…)` 防负）、`DELETE /{id}`（非本人 403「只能删除自己的回复。」）、`/report`（body `{reason}`，`target_type="greply"` 入库）
  - **举报处理**：ReportController/ReportDao 支持 `greply` 类型（管理员举报处理页能删 galgame 评论）
- **前端**：`GalgameDetailView.vue` 展示模式 `.gal-detail-card` 之后加评论区（照抄 PostView 评论区模板/样式，**去掉置顶**）；仅 `detail.status==='approved'` 显示（非 approved 提示「审核通过后可评论」）；`loadDetail` 支持 `{silent:true}` 静默重拉（发/删评论不闪骨架屏）。
- ⚠️ **Long 包装类 `!=` 坑**：`reply.userId() != uid.get()` 是两个 Long **引用比较**恒判不等 → 作者删除自己评论也 403。必须 `!reply.userId().equals(uid.get())`（帖子版 ReplyController 是 `Long != long` 基本类型比较所以没踩到）。
- 冒烟：`D:\claude code\galgame_comment_smoke.py`（本地 pymysql 模拟审核）**30/30 PASS**（发评论/未登录401/嵌套回复快照/空+超长+父不存在+pending 400/点赞 toggle/点踩独立/详情注入 liked/disliked/匿名无 liked/删除权限/父删子保留/举报 greply）。⚠️ GalgameReply 有 `@JsonInclude(NON_NULL)`，parent_id 为 null 时 JSON 省略该键（前端用 `r.parent_id` 直接判空即可）。

## 评论区增强三功能（已完成本地并部署线上，2026-08-29）
- **① 评论发图片（帖子+galgame 共用）**：
  - 新接口 `POST /api/comment-images`（`CommentImageController`，手动 `resolveUserId` 鉴权，未登录 401；multipart 字段 `file`；仿 GalgameController 封面：扩展名白名单 png/jpg/jpeg/webp/gif、≤10MB、存 `uploads/comment_images/`、文件名 `cimg_<nowToken>_<uuid32>.<ext>`、返回 `{"url":"/uploads/comment_images/<stored>"}`）
  - 表：`replies`、`galgame_replies` 各加 `images TEXT NULL`（存 **JSON 数组**字符串，如 `["/uploads/comment_images/a.png"]`）。DatabaseMigrator 两处 `ensureColumn`
  - 评论创建接口 body 加可选 `images: [url...]`（`POST /api/posts/{id}/replies` 的 `createReplyJson` + `POST /api/galgames/{id}/replies` 的 `createReply`）：每张必须 String、trim 非空、**以 `/uploads/` 开头**、≤500、**条数 ≤9**，否则 400「图片参数不合法。」；无图片传 null（JSON 里省略）
  - DAO：ReplyDao/GalgameReplyDao 的 `BASE_COLUMNS` 加 images、RowMapper 用 Jackson 3 `tools.jackson.databind.ObjectMapper` 反序列化（解析失败→null 不崩溃）、`insert` 加 `imagesJson` 参数
  - 前端：PostView.vue + GalgameDetailView.vue 评论区输入区 emoji 旁加 `🖼️` 图片按钮（隐藏 `<input type=file multiple>`，逐张 `FormData`→`/comment-images` 拿 url push 进 `replyForm.images`，缩略图可移除）；提交时 body 带 images；渲染在 `.reply-content` 下方 `.reply-images`（点击 `window.open` 新窗口）；样式 `.image-toggle`/`.reply-image-previews`/`.reply-images img`
  - ⚠️ 删除评论**不清理磁盘图片文件**（孤儿，与 galgame 封面上传行为一致）
- **② galgame 评论昵称右侧显示作者评分**：
  - 后端：`GalgameReply` record 加 `Double rating`（NON_NULL 省略）；`GalgameReplyDao.findRatingsByUsers(galgameId, userIds)` 批量查 `galgame_ratings`（`WHERE galgame_id=? AND score IS NOT NULL AND user_id IN(...)`）；`getById` 在 liked/disliked 注入**之外**（**匿名也注入**，评分是评论作者属性与浏览者是否登录无关）给每条 `withRating(r.userId()!=null ? ratingByUser.get(r.userId()) : null)`
  - **语义**：评分是「该评论作者对当前 galgame 的评分」——作者评分后**其所有评论**（含评分前发的旧评论）旁都显示；从未评分作者 `rating` 省略
  - 前端：`GalgameDetailView.vue` `.reply-author` 之后加 `★ {{ r.rating }}`（`.reply-rating`，金色 `#e6a23c`，`v-if="r.rating != null"`）
- **③ galgame 评论框「评论/长评」切换（仅 UI，功能未实现）**：`GalgameDetailView.vue` `.reply-form` 内 MentionTextarea 前加 `el-radio-group`（`el-radio-button label="comment"`「评论」/ `label="long"`「长评」），`replyType` ref 默认 comment；提交逻辑不受影响；placeholder 随选择变化（写下你的评论/长评）
- 冒烟：`D:\claude code\galgame_comment_enhance_smoke.py`（本地）**30/30 PASS**（未登录上传401/上传200/静态访问200/非图片400/帖子评论带图201+详情回显/外链images400/11张400/空数组省略/galgame 评论带图/评分注入：A评分后旧评论同步显示 rating、从未评分作者省略、匿名详情也显示 rating/非 /uploads/ 开头400/11张400）
- ⚠️ **长文本换行**：纯文本容器（`.reply-content`/`.content`/`.poll-desc`/`.profile-bio`）必须 `white-space: pre-wrap` + **`word-break: break-word`**，否则连续数字/字母串（如 300 个 1）被浏览器当单个词**不换行溢出**（中文每字天然可断行所以正常）。2026-08-29 修复 5 处（PostView/GalgameDetailView 评论、帖子正文、投票描述、个人简介）；`CompanyDetailView`/`ConversationView` 本就是正确模式可参考

## Galgame 评论短评/长评分类（已完成本地并部署线上，2026-08-29）
- **表**：`galgame_replies` 加 `is_long TINYINT(1) NOT NULL DEFAULT 0 COMMENT '1=长评(>300字)，0=短评'`（schema.sql + DatabaseMigrator `ensureColumn`，幂等）
- **model**：`GalgameReply` record 加 `Boolean isLong`（content 后），`core()`/`withLikeState()`/`withRating()` 全部透传；SNAKE_CASE → JSON 键 `is_long`，NOT NULL 列所以 **true/false 都输出**
- **DAO**：`GalgameReplyDao` BASE_COLUMNS/RowMapper 读 `is_long`；新 insert 签名 `insert(galgameId, author, content, isLong, imagesJson, userId, parentId, parentAuthor)`（`ps.setBoolean` 与 PollDao 的 TINYINT(1) 风格一致）
- **Controller**：`GalgameController.createReply` 解析 body `is_long`——**仅 `Boolean true` 视为长评**，缺省/非布尔一律 false；校验：长评 `content.trim().length() <= 300` → 400「长评至少需要 300 字。」，仍受 ≤2000 上限；短评照旧（非空、≤2000）
  - ⚠️ **类型是显式标记，不按长度推断**：短评可传 301 字（不传 is_long 就是短评）
- **前端** `GalgameDetailView.vue`：
  - 查看：评论列表上方 `.reply-view-toggle` el-radio-group「短评 (N)/长评 (N)」，`viewType` ref 默认 'short'；`shortReplies`/`longReplies`/`filteredReplies` computed 按 `r.is_long` 分流（旧数据无字段视为短评）；空态文案区分（还没有短评/还没有长评）；el-card header 评论总数不变
  - 发送：输入区切换文案「评论」→「短评」、值 `'comment'`→`'short'`（`replyType` 值域 short/long），placeholder 随选择变；`submitReply` 长评时 `content.length < 301` → `ElMessage.warning('长评至少需要 300 字。')` 并 return（前端拦截，服务端仍冗余校验）；body 仅长评带 `is_long: true`
- 冒烟：`D:\claude code\galgame_comment_shortlong_smoke.py`（本地）**14/14 PASS**（短评201 is_long=false/长评301字201 is_long=true/长评300字400「长评至少需要 300 字。」/长评短内容400/长评2001字400/短评301字201仍 is_long=false（类型显式）/详情 is_long 字段正确）

## 昵称功能（已完成，本地与线上 2026-08-28 已同步）
- **存储**：`users.nickname` VARCHAR(32)（**可重复**，初始=账号名）。迁移：DatabaseMigrator `ensureColumn` + `backfillNickname()` 回填存量（`SET nickname=username WHERE NULL/''`），新注册 `UserDao.insert` 直接写 `nickname=username`。schema.sql 的 users 表已加列定义。
- **帖子/评论实时显示昵称**：PostDao / ReplyDao 查询 `LEFT JOIN users u ON u.id = posts.user_id`，author 用 `COALESCE(u.nickname, posts.author) AS author` 实时取昵称（改昵称全站生效，匿名帖 / 用户被删回退快照列）；回复的 `parent_author` 同理实时化（再 JOIN 父回复 `replies pr` + `users pu`）。
  - ⚠️ **LEFT JOIN 后裸列歧义**：users 也有 id/created_at，Base 列、WHERE、ORDER BY 必须加 `posts.`/`replies.` 前缀（findAll 的 q 搜索里 author 也要改成 `COALESCE(u.nickname, posts.author) LIKE ?`；findFavoritesByUser 独立 SQL 需单独 JOIN）。
- **写入快照**：发帖/回帖 `PostController` 的 author 用 `user.nickname()`。
- **改昵称**：`PUT /api/auth/profile` 带 `nickname`（部分更新，非空、按 `codePointCount` ≤32，可重复不查重）。
- **模型/DAO**：User / UserProfile record 加 `nickname`；UserDao / FollowDao 所有 SELECT 带 nickname 列；`findByNameLike` 同时搜账号名+昵称（`username LIKE ? OR nickname LIKE ?`）。
- **前端**：ProfileView 编辑资料区「昵称」输入框（保存 PUT /auth/profile 并 `setUser` 更新）；所有显示用户名的位置（导航栏 / 搜索用户 / 关注列表 / 私聊 / 通知 / 收藏页）统一「昵称优先、账号名兜底」`X.nickname || X.username`。
- **部署脚本**：`D:\claude code\deploy_galgame_nickname.py`（paramiko 上传 jar + dist 原子替换 + 重启 8081 + 轮询验证），含服务器凭据仅本地使用不外传。

## 四功能：点踩 / emoji / 私信撤回 / 帖子封面（已完成，本地与线上 2026-08-28 同步部署）
- **点踩**（帖子 + 评论）：表 `post_dislikes` / `reply_dislikes`（仿点赞表），列 `posts.dislike_count` / `replies.dislike_count`。**与点赞独立不互斥**（可同时赞和踩）。接口 `POST /api/posts/{id}/dislike`、`POST /api/replies/{id}/dislike`（拦截器已注册，未登录 401），返回 `{disliked, dislike_count}`，toggle 减用 `GREATEST(0, ...)`。前端按钮灰色区分粉色点赞。
- **emoji**：纯前端（帖子正文 / 评论 / 私聊输入区），`EmojiPicker.vue` 组件（160 个、8 列网格、`emit('pick')` 追加到草稿）。内容字段 utf8mb4 直接存 emoji 字符，无需后端改动。
- **私信撤回**：列 `dm_messages.is_recalled`，接口 `POST /api/dm/messages/{id}/recall`（手动鉴权 requireLogin，未登录 401 / 非发送者 403「只能撤回自己发送的消息。」/ 不存在 404）。**两分钟窗口**：`created_at` 距今 > 2 分钟 → 403「发送超过两分钟，无法撤回。」；已撤回幂等返回当前消息不报错。前端 `ConversationView.vue` 撤回按钮 `v-if="canRecall(m)"`（自己发的 + 未撤回 + 距今 ≤ 2 分钟，4 秒轮询刷新后按钮自动消失）；已撤回气泡灰色斜体「已撤回」；`MessagesView` 会话预览显示「[已撤回]」。
- **帖子封面**：列 `posts.cover_image VARCHAR(500) NULL`，发帖 multipart 可选 `cover` 单文件（≤8MB，png/jpg/jpeg/webp/gif），存 `uploads/post_<id>/cover_<ts>_<uuid>.<ext>`（与附件同目录），发帖后回填 `updateCoverImage`。删帖时 `deleteAttachmentFiles` 一并清理。前端：发帖页封面上传（预览/移除/revokeObjectURL）；首页卡片缩略图与详情页封面均 `width:100%; height:auto`（左右和内容区同宽、上下自适应，整图完整显示不裁剪，不用 object-fit）。
- 冒烟脚本：`D:\claude code\smoke_test_4features.py`（注册两临时用户实测四功能 + 超时撤回 + 权限 401/403 + 幂等）。
- 部署脚本：`D:\claude code\deploy_galgame_features.py`（上传 jar+dist → 重启 8081 → 验证迁移 → 服务器本地 curl 冒烟 10 项），部署时已验证服务器 `post_dislikes/reply_dislikes` 表 + 4 新列全就位、Nginx 8080 反代正常。

## 置顶功能：管理员帖子置顶 + 仅楼主评论置顶（本地与线上 2026-08-28 同步部署）
- **帖子置顶**（`posts.pinned_until DATETIME NULL`，管理员设置截止时间、到期自动取消）：
  - 接口 `PUT /api/posts/{id}/pin` body `{days}`：days>0 置顶 N 天返回 `{ok, pinned_until}`；days=0 取消返回 `{ok, pinned_until:null}`；days<0 400「置顶天数不能为负。」；>3650 400「置顶天数不能超过 3650 天。」。仅管理员（admin_level≥1）：未登录 401 / 非管理员 403「需要管理员权限。」。拦截器已注册 `/api/posts/*/pin`。
  - 排序：`PostDao.findAll` 所有排序分支统一加前缀 `(posts.pinned_until IS NOT NULL AND posts.pinned_until > NOW()) DESC`（置顶中优先、过期自动失效）；收藏页 `findFavoritesByUser` 同样置顶优先。
  - 前端：HomeView / SectionView 帖子卡片右上角 `.pin-area`（管理员显示「置顶/取消置顶」按钮——未置顶 ElMessageBox.prompt 输正整数天数，已置顶 confirm 取消）；FavoritesView / ProfileView 仅显示红色「置顶」标签。`isPinned(p)` 工具函数（`utils/format.js`：`pinned_until` 存在且 > now）。
- **评论置顶**（`replies.is_pinned TINYINT(1) NOT NULL DEFAULT 0`，仅帖子楼主、无时间限制）：
  - 接口 `PUT /api/replies/{id}/pin` body `{pinned}`：仅楼主（`post.user_id == 当前uid`），**管理员若非楼主同样无权**，否则 403「只有帖子楼主可以置顶评论。」，返回 `{ok, pinned}`。拦截器已注册 `/api/replies/*/pin`。
  - 排序：`ReplyDao.findByPostId` → `ORDER BY replies.is_pinned DESC, replies.created_at ASC, replies.id ASC`（置顶排最前）。
  - 前端：PostView 评论区 `.reply-top` 红色置顶标签 + 按钮 `canPinReply(r)`（**仅楼主显示，楼主自己发的评论同样可置顶**）；toggle 后局部刷新 `is_pinned`。
  - ⚠️ JSON 键坑：Reply record 的 pinned 组件必须加 `@JsonProperty("is_pinned")`（否则 SNAKE_CASE 输出 `pinned`，前端读 `r.is_pinned` 会失效）。
- **模型/DAO**：Post 加 `LocalDateTime pinnedUntil`（NON_NULL 下未置顶不序列化）；Reply 加 `boolean pinned`；RecentPost 加 `pinnedUntil`（资料页近期帖显示标签）。
- **迁移**：DatabaseMigrator `ensureColumn("posts","pinned_until","DATETIME NULL")` + `ensureColumn("replies","is_pinned","TINYINT(1) NOT NULL DEFAULT 0")`，schema.sql 同步加列（幂等）。
- 冒烟脚本：`D:\claude code\smoke_test_pin.py`（28 项：401/403 权限、普通楼主置顶评论、管理员非楼主置顶评论 403、楼主自己评论可置顶、帖子置顶/取消/非法天数、置顶优先排序）。本地已 28/28 通过。
- 部署脚本：`D:\claude code\deploy_pin_to_server.py`（上传 jar+dist → 重启 8081 → 验证 2 列迁移 → 服务器 curl 冒烟 14 项），部署时已验证服务器 `posts.pinned_until`/`replies.is_pinned` 就位、公网 8080 反代正常。

## 投票功能：帖子投票（本地与线上 2026-08-28 同步部署，行为对齐 kungal 论坛）
- **3 张新表**：`polls`（主表：title/description/type(single|multiple)/min_choice/max_choice/deadline/status(result_visibility: always|after_vote|after_deadline)/is_anonymous/can_change_vote/post_id/user_id，user_id **NOT NULL + ON DELETE CASCADE**）；`poll_options`（text + vote_count 冗余计数）；`poll_votes`（唯一键 `(poll_id, option_id, user_id)` 防重，FK 全级联）。
- **接口**（6 个，`PollController`）：`GET /api/posts/{id}/polls`（可选登录）、`POST /api/posts/{id}/polls`（创建，仅楼主）、`PUT /api/polls/{id}`（编辑，仅楼主）、`DELETE /api/polls/{id}`（仅楼主）、`POST /api/polls/{id}/vote`（body `{option_id_array:[]}`，登录）、`GET /api/polls/{id}/logs?page&page_size`（仅楼主）。**所有 poll 路径不注册拦截器**，Controller 手动 `tokenService.resolveUserId(Authorization)` 鉴权。
- **权限（延续评论置顶规则）**：仅帖子楼主可管理投票（创建/编辑/删除/日志），**管理员非楼主同样 403**「只有楼主可以管理投票。」；投票需登录；列表公开。楼主判断 `post.user_id == 当前uid`。
- **canViewResults**（结果可见性，`PollController.canViewResults`）：楼主恒可见；结束（status=closed 或过 deadline）可见；否则按 result_visibility：always 可见 / after_vote 需已投 / after_deadline 需已结束。结果不可见时：`options[].vote_count` 省略、`vote_count`/`voters` 省略（NON_NULL）。
- **匿名投票**：`voters`（前 5 个投票人头像）一律省略，但 `voters_count`（去重人数）/票数照常返回；日志不返回 user。
- **投票/改票事务**：`PollDao.vote` 先删旧票逐选项减 `vote_count`，再插新票加计数；不可改票（can_change_vote=false）重复投 400「该投票不可修改投票。」。编辑时**已有票的选项不能改文本/删除** 400；can_change_vote=false 时只允许改标量。
- **模型/DAO**：`Poll` record（vote_count 用 `Integer` 可空 + NON_NULL，不可见时省略）；`PollOption` record（id/text/vote_count(Integer)/is_voted）；`PollDao`（BASE_COLUMNS + RowMapper + @Transactional + GeneratedKeyHolder，批量查 users 防 N+1）。
- **前端**（`frontend/src/components/`）：`PostPoll.vue`（投票区容器，独立 `GET /posts/{id}/polls` 拉取，仅 isOwner 显示创建入口）。**无投票时不渲染投票栏**：非楼主整个组件隐藏；楼主仅显示居中小「创建投票」按钮（`poll-create-inline`，2026-08-28 优化，避免空卡片占大位置）、`PollItem.vue`（单选 el-radio / 多选 el-checkbox、进度条+票数+百分比、投票/改票、结果可见性判断、楼主编辑/日志/删除）、`PollCreatorDialog.vue`（创建/编辑共用，编辑只发 add/update/delete）、`PollLogDialog.vue`（分页日志）。PostView 正文卡与评论卡之间挂 `<PostPoll :post-id :is-owner>`。结果可见性前端判断与后端 canViewResults 一致。
- **迁移**：schema.sql 建 3 表 + DatabaseMigrator `ensurePollTables()`（ensureTableIfAbsent 兜底旧库）。
- 冒烟脚本：`D:\claude code\smoke_test_poll.py`（76 项：权限/单选多选 min/max/投票改票计数/三种结果可见性/匿名/截止关闭拒绝/选项锁/日志分页/级联清理）。本地 76/76 通过。
- 部署脚本：`D:\claude code\deploy_poll_to_server.py`（上传 jar+dist → 重启 8081 → 验证 3 表迁移 → 服务器 curl 冒烟 18 项），部署时已验证服务器 3 表就位、公网 8080 反代正常。⚠️ 服务器 bash 冒烟里从创建响应提取选项 id 时**必须先截出 `"options":[...]` 段再取 id**（user 对象里也有 `"id"`，直接 grep 全 JSON 会拿到楼主 user id 导致投票 400「存在无效的选项。」）。

## 萌点(积分)系统 + 每日签到（本地与线上 2026-08-28 已同步部署）
- **数据**：`users` 加列 `moe_points INT NOT NULL DEFAULT 0`（DatabaseMigrator `ensureColumn` 兜底旧库）；新表 `daily_rewards`（`user_id/action_type('check_in'|'post'|'reply')/action_date`，**唯一键 `(user_id, action_type, action_date)` + INSERT IGNORE 防每日重复奖励**，并发安全，FK 用户级联删）。
- **接口**（`CheckInController`，路径未注册拦截器、手动 `tokenService.resolveUserId` 鉴权，未登录 401「请先登录。」）：`POST /api/check-in`（每日首次 +10，**幂等**：今天已签过返回 `{ok:true, awarded:false}` HTTP 200 不重复加分；成功 `{ok:true, awarded:true, moe_points:N}`）、`GET /api/check-in/status`（`{today_checked_in, moe_points}`）。
- **发帖/评论奖励**：`PostController.doCreatePost` 成功后 `claimDailyReward(userId,"post",10)`、`doCreateReply` 成功后 `claimDailyReward(userId,"reply",5)`；**每日首次各奖一次**；`try-catch` 静默容错（奖励失败不阻断发帖/评论）。
- **增减萌点留的接口（未来消耗功能用）**：`UserDao.adjustMoePoints(userId, delta)`（`GREATEST(0, moe_points+delta)`，负值安全）；`UserDao.claimDailyReward(userId, actionType, amount)`（可扩展新每日奖励类型）。
- **返回结构**：User（登录/注册/me）与 UserProfile（`GET /api/users/{id}`）均新增 `moe_points`。⚠️ `FollowDao` 也有一份 USER_ROW_MAPPER + 2 条 SELECT，加列时**必须同步补 `moe_points`**（否则编译失败）。
- **前端**：`ProfileView.vue` 资料卡右上角 `.moe-box`（margin-left:auto）显示「✦ 萌点 N」；本人可见签到按钮（未签「签到 +10」/已签禁用「今日已签到」）；load 时本人调 `GET /check-in/status`，签到成功更新 profile 与 user store；深色模式 theme.css 补 `html.dark .moe-box`。
- 冒烟脚本：`D:\claude code\smoke_test_moe.py`（17 项：注册/me/profile 的 moe_points=0、签到幂等、status、发帖/评论每日首次奖励、重复不加、未登录 401），本地 17/17 通过。
- 部署脚本：`D:\claude code\deploy_moe_to_server.py`（上传 jar+dist → 重启 8081 → 验证迁移「1 1」→ 服务器 curl 冒烟 17 项），部署时验证通过、线上 8080 反代正常，冒烟测试帖/测试用户已清理。⚠️ 服务器 bash 冒烟里提取用户 id 的变量**不能用 `UID`**（bash 只读保留变量，赋值会失败导致 curl 请求 `/users/0` 404）；改用 `NEWID`。

## 发帖草稿（已完成本地与线上，2026-08-28）
- `ComposeView.vue` 发帖页「发表」旁新增「**保存草稿**」按钮 + 恢复/清除。
- **存储**：localStorage `galforum_post_draft_<uid>`（**按用户 id 隔离**，同萌点基线方案），**只存文字字段**（分区/标签/标题/正文），**附件/封面不存**（浏览器无法把 File 存进 localStorage，已选附件/封面时保存提示「需重新选择」）。
- **恢复**：进入发帖页且已登录时 `loadDraft()` 自动读草稿填充 + 提示「已恢复上次保存的草稿」；`categories.includes(d.category)` 校验非法分区回退默认板块；损坏 JSON 静默忽略。
- **清除**：手动「清除草稿」按钮（`v-if="hasDraft"`）只删草稿保留表单；发帖成功后 `clearDraft({silent:true})` 静默清理。
- 逻辑验证：`D:\claude code\draft_logic_test.js`（15 项：保存/恢复/非法分区回退/清空/发帖成功静默清理/损坏草稿/按用户隔离）15/15 PASS。
- 部署脚本：`D:\claude code\deploy_galgame_draft_to_server.py`（仅传 dist，后端未改）。

## @ 自动补全（本地与线上 2026-08-28 已同步部署）
- **需求**：发帖正文 / 评论输入框里打出 `@` → 弹出当前用户**关注的人**的列表（昵称 + 头像 + @用户名），鼠标/键盘（↑↓ Enter Esc）选中后，在光标处插入 `@用户名 `，发送后由现有 MentionService 解析触发对方通知。
- **后端**（`FollowController.toFollowResponse`）：关注/粉丝列表接口补 `m.put("nickname", u.nickname())`（原来只有 username，前端无法显示昵称）。⚠️ **修了一个隐藏 bug**：`FollowDao.findFollowing/findFollowers` 两条 SQL 拼接处 `u.created_at"` 与 `"FROM` **之间缺空格**（拼成 `created_atFROM`），任何 `GET /api/users/{id}/following|followers` 都 500；已加空格修复。
- **前端**：新组件 `frontend/src/components/MentionTextarea.vue`——原生 `<textarea>`（便于 `setSelectionRange` 控光标）+ @ 触发检测（`selectionStart` 前最后一个 `@`；@ 前是词字符→忽略邮箱/URL；@ 后含空白/非法字符→结束）；onMounted 拉 `/users/{我的id}/following`（未登录不拉、不弹）；下拉列表过滤 nickname/username，`@mousedown.prevent` 防失焦 + blur 延迟 150ms 关闭；插入 `@用户名 ` 后 nextTick 定位光标。props 与 el-input textarea 对齐（modelValue/placeholder/rows/maxlength/showWordLimit，字数统计右下角）。
- **集成**：`ComposeView.vue` 发帖正文、`PostView.vue` 评论框两处 `<el-input type="textarea">` 替换为 `<MentionTextarea>`（私信输入框不改，@ 通知只在发帖/评论解析）。深色模式 theme.css 补 `html.dark .mention-*` 条目。
- **前端两个坑（已修）**：① **@ 触发晚一拍**——`onInput` 里 `emit` 后立刻读 `props.modelValue` 是旧值（Vue props 下一 tick 才更新），只输入 `@` 检测不到；改用 `e.target.value`（DOM 同步值）。② **正文输入框变窄**——`el-form-item__content` 是 flex 容器，`.mention-textarea` 缺 `width: 100%` 导致收缩成内容宽（小方框）；已加 `width: 100%`。
- 冒烟：本地 Python 9 项（注册两人→各自设中文昵称→B 关注 A→`GET /users/B/following` 含 A 且带 A 昵称/avatar_url→`GET /users/A/followers` 含 B 带 B 昵称），9/9 通过。⚠️ 本地 curl 冒烟发中文 JSON body 会 400（Windows 终端编码问题），改用 Python urllib（UTF-8）或 JSON `\uXXXX` 转义。
- 部署：`D:\claude code\deploy_mention_to_server.py`（上传 jar+dist → 重启 8081 → 服务器 curl 冒烟 6 项：注册/设中文昵称/关注/following 带 nickname/followers 带 nickname），部署时 6/6 通过、公网 8080 反代正常、测试用户已清理。⚠️ 服务器 bash 冒烟里**用户变量不能叫 `A`/`B`**——`B=URL` 定义 API 地址后又 `B="mnt_b_$TS"` 覆盖，导致所有 `$B/auth/...` 变 404（与 UID 变量坑同类）；用 `UA`/`UB`。

## 服务器部署（腾讯云线上）
- **架构**：腾讯云 `62.234.76.178`（域名 `dnf88896.site`，备案待办，通过后切 80）。Nginx（**8080 已是 HTTPS**）托管 `frontend/dist` + 反代 `/api/`、`/uploads/` → 后端 jar（8081）→ MySQL 8.4（3306，库 `galgame`）。部署目录 `/opt/galgame/{frontend/dist, backend/{jar, app.log, uploads}}`。
  - ⚠️ **公网 8080 只认 HTTPS**：`http://...:8080` 返回 400（Nginx 明文访问 HTTPS 端口）。本机校验/curl 必须 `https://dnf88896.site:8080`（自签证书加 `-k`），否则像 deploy 脚本 assets 校验那样误报 400/FAIL（上传/重启/后端本地冒烟均正常）。Python urllib 也要 `ssl._create_unverified_context()`。
- **更新流程**：本地 `mvn -q -DskipTests package` 打包 → 用 paramiko 脚本（参考 Temp 下 deploy 脚本）上传新 jar / dist → 重启后端：先杀旧进程再 `cd /opt/galgame/backend && nohup java -jar galgame-backend-0.0.1-SNAPSHOT.jar --server.port=8081 > app.log 2>&1 &`。
  - ⚠️ **只带 `--server.port=8081`，不要带 CORS 参数**：jar 默认已全放行（见下）。
  - ⚠️ **pkill 自杀坑**：`pkill -f galgame-backend` 会匹配到执行命令的 shell 自身（命令行含该字符串）导致命令卡死。改用 `for pid in $(ps -ef | grep '[g]algame' | awk '{print $2}'); do kill $pid; done`。
- **CORS 约定（2026-08-28 统一）**：本地与服务器后端代码都默认 `*` 全放行（`application.properties` 的 `app.cors.allowed-origins=*` + `WebConfig.java` 的 `@Value` 默认值 `*`）。**改 CORS 就改代码、两边一起部署，不要再靠启动参数 `--app.cors.allowed-origins=*` 临时覆盖**。Nginx 保留 `proxy_set_header Origin "";`：线上 API 只允许同源读取（跨域访问读不到响应，同源使用无影响）。
- **数据库备份**：安全备份在服务器 `/root/backup/galgame_dump_YYYYMMDD.sql`（`mysqldump -ugalgame -pgalgame123 --single-transaction --no-tablespaces galgame`，权限 600）。旧敏感备份已删除，**不要再往 `/opt/galgame` 放备份文件**。
- **安全现状**：腾讯云安全组挡 3306/8081 公网入口（真防线，8081/3306 公网不可达）；宝塔面板已关（8888 无监听）；旧 jar 备份 `/root/backup/galgame-backend-OLD-20260828.jar`。MySQL 账号 `galgame`（密码与本地 `application.properties` 默认一致）。root SSH / API key 等敏感凭据只在服务器配置与本地脚本中使用，**绝不在任何文档/对话中外传**。
- **只读审计 Claude Code**：服务器上有审计账号 `audit`（工作区 `/opt/audit-workspace`，DeepSeek 接口），可作主会话只读子代理查服务/日志/数据库/找 bug。调用方式见全局记忆 [[server-audit-claude-code]]。

## 会社库（已完成本地与线上，2026-08-29 已同步部署）
- **定位**：Galgame 制作会社资料库，功能逻辑与 galgame 提交审核完全一致（用户创建 → 管理员审核 → 上架），但**精简字段**：只有 name/description/website/logo_image（logo 预留暂未开放上传，无标签/封面/发售日期/链接/评分）。
- **侧边栏**：HomeView `leftNavGroups` galgame 组第 3 项改为「会社」→ `/company`。
- **路由**（`router/index.js`）：`/company`（列表）、`/company/new`（提交）、`/company/review`（审核，在 `:id` 前）、`/company/mine`（我的提交）、`/company/:id`（详情）。
- **前端**：`CompanyView.vue`（列表：名称搜索 `q` + 排序 created 默认/views/views_asc 两态 `SORT_DIR`，顶栏三按钮「审核会社信息」warning/「我的提交」/「+ 添加会社」，卡片显示 name/desc/官网/浏览数）；`AddCompanyView.vue`（`!loggedIn` 挡板，表单名称+简介+官网，管理员创建即上架、普通用户提交审核，成功文案按角色区分 + `refreshMoe()`）；`CompanyReviewView.vue`（`!isAdmin` 挡板，`GET /companies/pending`，卡片含提交人/时间，空态「暂无待审核的会社提交」）；`CompanyMineView.vue`（`!isLoggedIn` 挡板，状态 tag+拒绝理由+提交时间，onMounted `refreshMoe()`，空态「你还没有提交过会社。」）；`CompanyDetailView.vue`（loading/notFound/loadError 分支，状态 tag+reject_reason el-alert，`canEdit`/`canDelete` computed 同 galgame：`isAdmin || (isCreator && status!=='approved')` / `(isAdmin && status!=='pending') || (isCreator && status==='pending')`，管理员+pending 时通过/拒绝按钮，内联编辑表单名称+简介+官网，`logo_image` 编辑时保留原值）。
- **后端**：`schema.sql` 建 `companies` 表（幂等，含 status/reject_reason/reviewed_at/moe_awarded，`created_by` FK ON DELETE SET NULL，同 galgames 审核模式）；`model/Company.java`（record：id/name/description/website/logoImage/viewCount/createdBy/createdAt/updatedAt/status/rejectReason/reviewedAt/creator）；`dao/CompanyDao.java`（BASE_COLUMNS 带 creator 子查询，findAll(q,sort) 仅 approved、findPending/findByCreator/updateStatus/claimMoeAward/incrementView/deleteById，orderBy 支持 created/views/views_asc）；`controller/CompanyController.java`（`/api/companies` 未注册拦截器，手动 `tokenService.resolveUserId` 鉴权；列表公开仅 approved、详情 pending/rejected 仅创建者/管理员否则 404、POST 登录用户管理员→approved/普通→pending、PUT 管理员全改/创建者非approved、DELETE 管理员全删/创建者pending、GET /pending 和 /mine 精确路径优先于 `/{id}`、POST /{id}/review adminGate 拒绝必填理由≤500 通过给提交者 +10 萌点 claimMoeAward 原子防重）。**注意**：company 的 getById/review 直接返回 Company record（不转 Map，无 rated/moe_granted 附加字段，比 galgame 简化）。
- **冒烟**：`D:\claude code\company_smoke.py`（无凭据，临时随机用户，21 项：未登录 401/提交 pending/公开列表隔离/pending 详情仅创建者/编辑删除权限矩阵/mine 字段，21/21 PASS）。管理员审核半边未在本地自动冒烟（需 admin 凭据），与 galgame review 同构、galgame 侧已 20 项 PASS。

## galgame 关联会社（「制作人员」→「会社」下拉 + 双向跳转，已完成本地与线上，2026-08-29 已同步部署）
- **存储**：`galgames` 加 `company_id BIGINT NULL`（**不加 FK 约束**，应用层校验；schema.sql + DatabaseMigrator `ensureColumn`）。`staff` 仍存公司名**快照**（列表展示 / 搜索 field=staff 照旧）；有 company_id 时 staff=该公司名（创建/编辑时后端自动生成）。
- **后端**：`Galgame` model 加 `companyId`（SNAKE_CASE → JSON 键 `company_id`）；`GalgameDao` BASE_COLUMNS/mapRow/`insert`/`update` 加 company_id，`findAll(q, tags, sort, field, Long companyId)` 末尾参数非空时追加 `AND g.company_id = ?`（仍仅 approved）；`CompanyDao` 加 `findApprovedNameById(id)`（`SELECT name WHERE id=? AND status='approved'`）；`GalgameController` 注入 CompanyDao，请求体 record 加 `companyId`，`list` 加 `@RequestParam company_id`，create/update 在 `normalizeAndValidate` 处理：**companyId 非空 → findApprovedNameById 查不到返回 400「所选会社不存在。」，查到用公司名覆盖 staff**；companyId 空 → 保留 body 的 staff（trim 可空，兼容旧客户端/历史数据）；staff≤200 长度校验仅在 companyId 为空时执行。
- **前端**：`AddGalgameView`「制作人员」输入 → 「会社」`el-select`（onMounted `GET /companies` approved，`clearable`+`filterable`，`:value="c.id"` `:label="c.name"`），payload 删 `staff` 改发 `company_id: form.companyId || null`；`GalgameDetailView` 展示模式 staff 有 `company_id` 时渲染 `.gal-staff-link` 点击 `router.push('/company/'+id)`（无 company_id 历史数据仍纯文本），编辑模式同下拉、`enterEdit` 回填 `form.companyId = d.company_id || null`。
- **会社详情作品栏**：`CompanyDetailView` 详情卡片下方「作品」栏——`GET /galgames?company_id={companyId}` 拉该会社**已上架**作品（加载中骨架 / 失败 el-alert / 空态「该公司暂无收录作品」），作品卡（封面 `image`/名称/简介 3 行省略/浏览/评分）整卡点击跳 `/galgame/{id}`；仅详情加载成功（非 404）时渲染。
- **冒烟**：`D:\claude code\company_galgame_link_smoke.py`（pymysql 直连本地库**模拟审核上架**、无管理员凭据，从 `application.properties` 读占位符配置，支持 `${DB_PORT:3306}` 形式），15/15 PASS：选会社创建 staff=公司名且返回 company_id / 不选会社兼容旧 staff 文本 / 选不存在会社 400「所选会社不存在。」 / PUT 改会社 staff 同步更新 / `?company_id=X` 只返回该会社 approved 作品 / 详情返回 company_id。⚠️ 该脚本 DB 直改 status 模拟审核（非真实审核），测试数据自清理；galgames 有效标签键如 `gg-type-game`（`gg-type-adv` 不存在会 400「无效的标签」）。

## 制作人员 / 角色库（已完成本地并部署线上，2026-08-29 已同步部署）
- **定位**：Galgame 制作人员（staffs）与角色（characters）资料库，功能逻辑与会社库一致（用户创建 → 管理员审核 → 上架，完整审核状态机 + reject_reason + claimMoeAward +10 萌点），并实现**三方互相指向**：galgame ↔ 制作人员、galgame ↔ 角色、制作人员 ↔ 角色均为多对多。
- **侧边栏**：HomeView `leftNavGroups` galgame 组 3-3/3-4 占位按钮改为「制作人员」→ `/staff`、「角色」→ `/character`。
- **路由**（`router/index.js`，各 5 条，/new、/review、/mine 均在 `/:id` 前）：`/staff`（列表）、`/staff/new`、`/staff/review`、`/staff/mine`、`/staff/:id`；`/character` 同构。
- **前端**：Staff 5 页照抄 Company 5 页（`StaffView` 列表 q 搜索+排序+顶栏审核/我的/添加；`AddStaffView` 未登录挡板+名称/简介+**关联角色多选**（GET /characters 下拉）提交 `{name, description, character_ids}`；`StaffReviewView`/`StaffMineView`；`StaffDetailView` 信息卡+**关联角色区块**（`detail.characters` 点跳 `/character/:id`，空态「暂无关联角色」）+**作品区块**（`GET /galgames?staff_id=`，点跳 `/galgame/:id`）+ 管理员+pending 通过/拒绝 + `canEdit` 编辑对话框带关联角色多选回填）；Character 5 页完全对称（body 用 `staff_ids`，详情显示关联制作人员，作品用 `?character_id=`）。
- **UI 改造（2026-08-29 本地，未部署）**：① `GalgameDetailView` 详情页「制作人员」「角色」从信息卡两行提升为**与评论区平级的 tab 栏**（`.gal-section-tabs`，三个大按钮 10px 22px「制作人员|角色|评论」，默认制作人员，`sectionTab` ref 切换）；每个 tab 项显示左侧灰色描述 + 右侧蓝色可点名称（`goStaff`/`goCharacter` 跳转），空态「暂无制作人员」「暂无角色」；评论区只在 tab=replies 且 `status==='approved'` 时渲染（评论区走 el-card + EP 变量自动深浅色）；tab 按钮 + 内容 pane 深浅色适配在 **theme.css 5.11b**（`html.dark .gal-section-tab/.gal-section-pane` → 深灰 `#1d1e1f` 边框 `#3a3a3c`，tab 文字 `#c0c4cc`，hover 主色+`#2a2a2c` 底，描述/空态 `#8c8c90`，投影 `rgba(0,0,0,.4)`）。② `GalgameDetailView` 编辑模式 + `AddGalgameView` 新建表单：制作人员/角色改为**资源链接式行式编辑**（左侧描述 el-input placeholder「职责/备注（如 脚本、原画）」/「定位/备注（如 女主、CV）」，右侧 el-select filterable clearable 选人，删除按钮，下方「+ 添加制作人员」/「+ 添加角色」按钮，复用 `.add-links` 样式）；表单字段 `staffLinks`/`characterLinks`（数组 `{id, description}`），编辑回填 `detail.staffs.map(s => ({id: s.id, description: s.description||''}))`，提交 payload 键 `staffs`/`characters`（`filter(l => l.id != null)` + trim description）。
- **后端 5 张表**（schema.sql + DatabaseMigrator 双写）：`staffs`、`characters` 实体表（仿 companies：name/description/view_count/created_by FK SET NULL/status/reject_reason/reviewed_at/moe_awarded，name 索引）；`galgame_staff(galgame_id, staff_id, description)`、`galgame_character(galgame_id, character_id, description)`（**description VARCHAR(200) NULL** = 制作人员在作品中的职责 / 角色在作品中的定位，2026-08-29 请求 D 新增）、`staff_character(staff_id, character_id)` 关联表（复合主键 + 双 FK **ON DELETE CASCADE** + created_at，约束名 fk_gs_*/fk_gc_*/fk_sc_*）。
- **后端代码**：`model/Staff.java`、`model/Character.java`（record 仿 Company）、`StaffBrief/CharacterBrief.java`（`{id, name, description}`，构造顺序 `(Long id, String name, String description)`）；`StaffDao`/`CharacterDao`（仿 CompanyDao 全方法 + `insertWithCharacters/updateWithCharacters`（@Transactional 先 delete 关联再 batchUpdate 全量替换）+ `findLinkedCharacters/findLinkedStaffs`（只回显 **approved** 关联）+ `characterExists/staffExists`）；`StaffController`/`CharacterController`（`/api/staffs`、`/api/characters` 未注册拦截器手动 token 鉴权，8 接口照抄 CompanyController：GET 列表仅 approved / GET {id} pending 仅创建者或管理员否则 404 + incrementView / POST 管理员 approved 普通 pending / PUT 管理员全改创建者非 approved / DELETE / GET pending+mine / POST {id}/review 拒绝必填理由通过 +10 萌点，**/pending /mine 在 `/{id}` 前**）。`GalgameDao`：insert/update 参数为 `List<GalgameStaffLink>/List<GalgameCharacterLink>`（事务内与 tags 一起先删后插，INSERT 带 description 列 `VALUES (?,?,?)`）、findAll 重载 7 参（staff_id/character_id 用 `EXISTS` 子查询过滤，旧 5 参委托保留）、findStaffBriefs/findCharacterBriefs（SQL SELECT 带 `gs.description`/`gc.description`）、staffExists/characterExists；`GalgameController`：`GalgameRequest` record **内嵌** `record StaffLinkReq(Long id, String description)`、`record CharacterLinkReq(Long id, String description)`（JSON 键 `staffs`/`characters`，对齐 `Galgame.Link` 写法），list 加 `@RequestParam staff_id/character_id`，getById 详情 Map 加 `staffs[]`/`characters[]`（每项 `{id, name, description}`，仅详情返回列表不加，旧数据 description 为 null），normalizeAndValidate 逐项校验 id 存在否则 400「所选制作人员不存在。」/「所选角色不存在。」、description ≤200 否则 400「描述不能超过 200 字。」、按 id 去重保序（LinkedHashMap.putIfAbsent 保留首现 description 防复合主键冲突）。
- **冒烟**：`D:\claude code\staff_character_smoke.py`（pymysql 直连本地库模拟审核上架），40/40 PASS：未登录 401 / staff+character 创建 pending / 列表隔离 / pending 详情仅创建者 404 / mine / 审核前 pending 关联角色不回显（只显示 approved）/ DB 上架后互相指向（staff↔character、galgame↔staff/character）/ **新协议 `staffs:[{id,description}]` 创建 galgame + 详情回显描述（「staffs带描述」「characters带描述」断言）/ 选不存在关联 400 / `?staff_id=` `?character_id=` 过滤 / PUT 改关联全量替换（pending 时可编辑，approved 不可）/ B 编辑删除 403 / 本次测试关联清 0（残留检查只查本次创建的 gid/staff_ids，**不全局断言**——库里可能有用户手动测试数据）**。⚠️ 测试用户前缀 `stf_` 残留库（与历史冒烟一致），数据行自清理。

## Galgame 标签系统（已完成本地并部署线上，2026-08-29）
- **定位**：复刻 KUNGal 标签体系——标签 = 实体表（name 唯一 + category 类别 + spoiler_level 剧透级 + 实时作品计数），详情页 = 资源 chips + 4 类标签区（chip 带计数/类别色/剧透角标/可筛选），编辑用远程搜索多选实体选择器 + 可新建标签；词表数据与交互数据分离。技术栈仍 Vue3+EP / Spring Boot+JDBC / MySQL（只借鉴 KUNGal 思路，不改栈）。
- **表**（schema.sql + DatabaseMigrator 双写，幂等）：`tags` 标签实体（id/name **UNIQUE** 中文/alias 旧 section_key `gg-*`/category/spoiler_level/description/created_by FK SET NULL/created_at/updated_at/status/reject_reason/reviewed_at/moe_awarded，仿 staffs 审核状态机）；`galgame_tag(galgame_id, tag_id)` 作品↔标签多对多（复合主键 + 双 FK **ON DELETE CASCADE** + created_at，约束 fk_gt_*）。旧表 `galgame_tags` 保留为**分类**存储（分类系统已恢复，见「Galgame 分类 + 标签分离」节）。
- **category 7 枚举**：`type`资源类型(详情信息卡资源 chips 蓝)/`language`语言(灰)/`platform`平台(绿) 三类资源类**不进标签区**；`content`游戏内容(蓝)/`meta`作品属性(绿)/`technical`技术细节(绿)/`sexual`成人内容(红) 四类进详情页**标签区**。
- **分类与标签数据分离**（DatabaseMigrator，2026-08-29 改）：启动时执行 `DELETE FROM tags WHERE alias IS NOT NULL` 清掉**之前误播种/迁移进标签表的 gg-* 分类数据**（幂等）；**不再播种 24 个 gg-*、不再把旧 `galgame_tags` 迁进 `galgame_tag`**。分类完全由旧表 `galgame_tags` + `TagConstants` 负责，标签表只存真正的标签实体。
- **后端**：新 `model/Tag.java`（完整审核实体）、`model/GalgameTagBrief.java`（`{id,name,category,spoilerLevel,galgameCount}`）、`dao/TagDao.java`（insert/update/findById/existsById/nameExists/findAll(q,category,sort 含 count)/search(q,limit)/findPending/findByCreator/updateStatus/claimMoeAward/deleteById）、`controller/TagController.java`（路由 **`/api/galgame-tags`** 避免与帖子 `/api/tags` 冲突；未注册拦截器手动 `tokenService.resolveUserId` 鉴权，8 接口照抄 StaffController，**/pending /mine 在 `/{id}` 前**；normalizeAndValidate：name 必填≤50 唯一（400「标签已存在。」）、category 7 枚举、spoiler_level∈{0,1,2}、description≤200）。`GalgameDao`：BASE_COLUMNS tags 子查询改为 **JSON 对象数组**（`CONCAT('{...JSON_QUOTE(t.name)...}')` GROUP_CONCAT，mapRow 用 Jackson 3 `tools.jackson.*` JsonMapper 解析）、insert/update 参数 `List<Long> tagIds`（先删 galgame_tag 后 batchInsert 同事务）、findAll tags 过滤改 **tag_id** `EXISTS`、新增 `findTagsByGalgame(galgameId)`（含全站 `galgame_count` 子查询按 category,id 排序）+ `tagExists`；`Galgame.java` tags 字段 `List<String>`→`List<GalgameTagBrief>`；`GalgameController` `GalgameRequest` 新增 `List<Long> tagIds`（JSON `tag_ids`）**移除旧 `tags`**、list `@RequestParam tags` 改 List<Long>、normalizeAndValidate 逐项校验存在且 status='approved' 否则 400「所选标签不存在或未通过审核。」、LinkedHashSet 去重保序。
- **前端**：`constants/galgameTag.js`（CATEGORY_LABELS/OPTIONS、SPOILER_LABELS、TAG_SECTION_FILTERS 全部/内容/属性/技术/成人、RESOURCE_CATEGORIES、TAG_SECTION_CATEGORIES）；新 `components/GalgameTagSelect.vue`（el-select remote 远程搜索多选 + 下拉尾部**「新建标签」**入口弹窗 POST /api/galgame-tags，普通用户提交 pending 提示待审核，`@mousedown.stop.prevent` 防 #empty slot 失焦，remote 模式 setup 顶层 `searchTags('')` 预载全部 approved + `preloadOptions` prop 回填编辑已有标签，选中值 `String(t.id)` 保证严格比较一致）；新 `views/GalgameTagView.vue` 标签库页（搜索 + 按 category 分组网格）/ `GalgameTagDetailView.vue`（标签名/类别 chip/剧透级/描述/计数 + works 作品列表）；路由 `/galgame/tag`、`/galgame/tag/:id`（在 `/:id` **前**）+ 侧边栏「标签」入口；`GalgameDetailView.vue`：信息卡 meta 区资源 chips（`.gal-resource-chip.res-{category}` type 蓝/language 灰/platform 绿，点击 goTag）+ 简介下「标签」区（类别筛选 chip + 「显示剧透标签」checkbox 默认关只显 spoiler_level=0 + `.gal-tag-chip` 网格 `{name} +{galgame_count}` + 剧透角标 + 空态「暂无标签」），编辑模式 `<GalgameTagSelect v-model="form.tagIds" :preload-options="detailTagObjects" />` 提交 `tag_ids`；`GalgameView.vue` 卡片显示**分类**（`categories` → categoryLabel）、四行筛选用分类常量（见「分类 + 标签分离」节）；Add/Mine/Review 同步 `tagIds` + `categories`；theme.css 补标签区/chip 深色覆盖（html.dark 模板）。
- **冒烟**：`D:\claude code\galgame_tags_smoke.py`（pymysql 直连本地库模拟审核上架，无管理员凭据），**25/25 PASS**（分类+标签并存，详见「分类 + 标签分离」节）。⚠️ search q 参数中文必须 `urllib.parse.quote`（否则 urllib 报 InvalidURL 含空格）；标签测试前缀 `TGT` 残留库时先清 `DELETE FROM galgames/tags WHERE name LIKE 'TGT%'` + `users WHERE username LIKE 'tgt_%'` 再重跑（时间戳每次不同不会撞唯一键）。

## Galgame 分类 + 标签分离（已完成本地并部署线上，2026-08-29）
- **定位**：两个体系彻底分离并存——**分类**（旧系统恢复，大方向，标在 galgame **列表页**）+ **标签**（新 KUNGal 风格，细致方向，显示在**详情页**）。用户明确分类不再叫「标签」。
- **分类**：旧表 `galgame_tags(galgame_id, section_key)` 存 24 个硬编码 `gg-*`（类型 type 8 / 语言 language 5 / 平台 platform 6 / 作品分类 work 5，中文映射在 `constants/TagConstants.java`，`isSectionKey()` 校验）。作用范围：**列表页四行筛选 + 卡片显示 + 编辑表单分类多选**。创建/编辑 galgame payload 带 `categories: string[]`（section_key）。
- **标签**：新表 `tags` + `galgame_tag`，`tag_ids` 提交，详情页资源 chips + 标签区 + GalgameTagSelect 多选 + 标签库/标签详情页（详见上节）。
- **后端**：`Galgame` record 加 `List<String> categories`（JSON `categories`）；`GalgameDao` BASE_COLUMNS 加分类子查询 `(SELECT GROUP_CONCAT(gt3.section_key ORDER BY gt3.section_key) FROM galgame_tags gt3 WHERE gt3.galgame_id = g.id)`（**别名 gt3 避让** tags 子查询的 gt2）+ `parseCategories`（逗号拆分）、insert/update 加 `List<String> categories`（写旧表，`insertGalgameCategories`/`deleteGalgameCategories` 同事务先删后插）、findAll 8 参实现 `findAll(q, tags, categories, sort, field, companyId, staffId, characterId)`（5/7 参委托 categories=null；分类过滤 `EXISTS (SELECT 1 FROM galgame_tags gt WHERE gt.galgame_id=g.id AND gt.section_key=?)` AND 语义，tags 参数仍是 tag_id）；`GalgameController` `GalgameRequest` 加 `List<String> categories`、list 加 `@RequestParam categories`、normalizeAndValidate 逐项 `TagConstants.isSectionKey` 否则 400「无效的分类。」（LinkedHashSet 去重保序）；`TagConstants` 未动（仍被 PostController 用）。
- **前端**：新 `constants/galgameCategory.js`（`GALGAME_CATEGORIES` 24 项 `{key,label,group}` **label 以 TagConstants 实际为准**、`CATEGORY_GROUPS` 四组 type/lang/plat/work、`categoryLabel(key)`）；`GalgameView` 卡片渲染 `p.categories`→categoryLabel（slice(0,4) + `+N`）、四行筛选用常量同步构建（值=section_key，load 传 `categories` 数组→`galParamsSerializer` 重复 key）、**不再从 `/api/galgame-tags` 拉筛选选项**；`GalgameDetailView` 展示模式 meta 区加分类行（`.gal-tag` 纯展示不可点，无分类不渲染）、编辑模式「分类」分组多选（el-select multiple + `el-option-group` 四组）在「标签」字段前、form 加 `categories` + enterEdit 回填 + payload 加 `categories`；`AddGalgameView` 同；Mine/Review 卡片渲染分类（MineView 状态 el-tag 保留）。
- **数据清理**：旧库曾把 gg-* 播种进 tags 表并迁移 galgame_tags 到 galgame_tag，已通过 DatabaseMigrator `DELETE FROM tags WHERE alias IS NOT NULL` 清掉（级联删 galgame_tag 关联），**标签表与分类彻底分离**；旧表 galgame_tags 数据仍完整。
- **冒烟**：`D:\claude code\galgame_tags_smoke.py` **25/25 PASS**：未登录建标签 401 / A 建标签 pending / mine / 选 pending 标签 400 / **非法分类 400「无效的分类。」** / 带 `categories`+`tag_ids` 建 galgame 201 / **详情 categories 回显** / 详情 tags 对象数组 / **列表 `?categories=` 单/多 AND 过滤** / 标签计数 / tag 详情 works / 选不存在 tag 400 / B 编辑删除 403 / **标签表无 gg-* 种子（0）+ galgame_tag 无 gg-* 关联（0）+ 分类存旧表 galgame_tags（2 行）** / 本次数据清 0。
- ⚠️ `TagConstants` 里 gg-lang-others/gg-plat-others 的 label 也是「其它」（三个「其它」），前端常量按后端原样使用（与旧系统显示一致）。
- **部署**：`D:\claude code\deploy_galgame_tags_categories_to_server.py`（上传 jar+dist → 重启 8081 → 服务器冒烟 22 项 → 清理 → assets 校验）。部署时 22/22 PASS、assets 4/4、home/api 200。⚠️ 服务器冒烟两个坑：① 服务器 python3 heredoc 里用了 `time.time()` 必须 `import time`（模板有，漏抄会 NameError）；② pending 状态的 galgame 详情**非创建者访问返回 404**——断言详情 categories/tags 必须带创建者 token，否则 `d` 是 error dict 导致 `d.get('categories')` 为 None、详情断言全 FAIL。

## Galgame 画廊 + 相关系列（已完成本地并部署线上，2026-08-29）
- **定位**：详情页两个新模块——**画廊**（tab 栏「评论」右侧新增「画廊」按钮，可编辑上传多图）+ **相关系列**（详情页页面最底部「相关系列」框，指向别的 galgame 详情页，可添加多个）。
- **表**（schema.sql `ENGINE = InnoDB` 带等号空格 + DatabaseMigrator `GALGAME_IMAGES_DDL`/`GALGAME_RELATED_DDL` `ENGINE=InnoDB` 无空格双写，`ensureTableIfAbsent` 幂等 + 3 个 `ensureFk` 兜底）：
  - `galgame_images(id, galgame_id FK CASCADE, url VARCHAR(500), sort_order INT DEFAULT 0, created_at, KEY idx_gi_galgame)` 画廊图。
  - `galgame_related(galgame_id FK CASCADE, related_id FK CASCADE, created_at, PRIMARY KEY(galgame_id, related_id), KEY idx_gr_related)` 双 FK 都指向 galgames。
- **后端**：新 `model/GalgameImage.java`（`{id,url}`）；`GalgameDao` 8 个新方法：`findImages`（ORDER BY sort_order,id）/`insertImages`（batchUpdate 按序赋 sort_order）/`deleteImage`/`findImageGalgameId`（删图前查归属）/`insertGalgameRelated`+`deleteGalgameRelated`（先删后插，复用 galgame_staff 模式）/`relatedExists`（`id AND status='approved'`）/`findRelatedGames`（`galgame_related gr JOIN galgames g ON g.id=gr.related_id` 仅 approved，复用 BASE_COLUMNS 的 `g.` 别名）；`insert`/`update` 加 `List<Long> relatedIds` 同事务先删后插。`GalgameController`：`GalgameRequest`/`NormalizedGalgameRequest` 加 `relatedIds`（JSON `related_ids`）、`normalizeAndValidate(body, selfId)`（**签名加了 selfId**——create 传 null、update 传 path id；relatedIds 逐项校验存在且已上架否则 400「所选的相关系列不存在或未上架。」、`relatedId.equals(selfId)` → 400「不能关联自身。」、LinkedHashSet 去重保序）、`getById` Map 在 tags 后加 `gallery`+`related_games`（**所有状态都返回**）、画廊上传 `POST /api/galgames/{id}/images`（multipart 字段 `files` 数组；登录+权限「管理员随时可传 / 创建者 status≠approved 可传」否则 403；逐张校验空跳过/>10MB 400/扩展名白名单否则 400；文件名 `galgame_gallery_<nowToken>_<uuid32>.<ext>` 存 `uploads/galgame_images/` 与封面同目录；返回全部画廊图）。新 `controller/GalgameImageController.java`（`@RequestMapping("/api/galgame-images")` 未注册拦截器手动 token 鉴权）：`DELETE /api/galgame-images/{id}`（`findImageGalgameId` 不存在 404「画廊图片不存在。」、权限同上 403、返回 `{"ok":true}` 对齐 DELETE /galgames 风格）。
- **前端**（`GalgameDetailView.vue` + `theme.css` 5.11c 深色覆盖）：`sectionTabs` 末尾加 `{key:'gallery',label:'画廊'}`（评论右侧）；画廊 pane `.gal-gallery-grid`（auto-fill minmax(160px,1fr)）每项点击 `openGalleryImage` 新标签看大图 + `canEdit` 时右上角「删除」；`canEdit` 时「上传图片」按钮 + 隐藏 `<input type=file multiple>`（**复用评论多图模式**：逐张 FormData `files` 串行 POST `/galgames/{id}/images`，成功 `loadDetail({silent:true})`，finally 清 input value）；底部「相关系列」框 `.gal-related-section`（卡片含封面 `resolveAssetUrl`/「无封面」占位 + 名称单行省略，点击 `router.push('/galgame/'+id)`，空态「暂无相关系列」）插在评论 hint 之后、报告 dialog 之前、**展示模式始终显示**；编辑表单「相关系列」el-select multiple filterable remote `:remote-method="searchRelatedGames"`（GET `/galgames?q=` 映射 `{id,name}`，**enterEdit 必须预载 `relatedOptions`**——remote 模式已选 id 无 label 的坑同 GalgameTagSelect）+ `form.relatedIds` 回填 + payload `related_ids: form.relatedIds.filter(Boolean)`。
- **权限**：画廊上传/删除按钮 `v-if="canEdit"`（`isAdmin || (isCreator && status!=='approved')`），后端同步内联判断；已上架作品创建者不能再传/删图（403）。相关系列随 create/update 的现有权限走（approved 后创建者不能编辑，403）。
- **冒烟**：`D:\claude code\gallery_related_smoke.py`（pymysql 直连本地库模拟上架）**22/22 PASS**：未登录传画廊 401 / A 建 pending 传 2 图 200 / 详情 gallery 含 2 图+url / 磁盘有 `galgame_gallery_` 文件 / B 传 A 的图 403 / **上架后 A 传图 403** / B 删 A 图 403 / 上架后 A 删图 403 / 关联未上架 400 / 关联不存在 400 / 关联自身 400 / B 编辑 A 403 / A 关联 g2 200 / 详情 related_games 含 g2+名称 / 单向（g2 详情不含 g5）/ create 带 related 201+回显 / 关联表清 0。⚠️ **冒烟坑**：画廊测试会把 g1 上架测 403，相关系列测试必须另建 **pending 载体**（如 g5）做「创建者编辑自己未上架」断言，不能复用已上架的 g1（否则 A PUT g1 全 403）。⚠️ Windows curl 多图上传 `-o` 要用 `NUL`（Git Bash 的 `/dev/null` 对原生 curl.exe 是 write error exit 23）。
- **部署**：`D:\claude code\deploy_gallery_related_to_server.py`（上传 jar+dist → 重启 8081 → 服务器冒烟 21 项含 gallery/related + 分类回归 → 清理测试数据+磁盘 `galgame_gallery_` 图 → assets 校验）。部署时 21/21 PASS、assets 4/4、home/api 200。⚠️ 服务器冒烟坑：测试 PNG 在 python3 heredoc 里用 `bytes.fromhex` 生成 `/tmp/gr_1x1.png`；curl 上传 `-F files=@...` 服务器端字段名是 `files`；清理必须 `rm -f /opt/galgame/backend/uploads/galgame_images/galgame_gallery_*.png`（后端删图不删磁盘）。

## 四实体「修改申请」（影子行）+ 审核标注 + 红点计数（已完成本地并部署线上，2026-08-29）
- **定位**：galgame / 会社 / 制作人员 / 角色四实体，用户原本只能提交**创建申请**、成功后不可改；现改成**任意登录用户对已上架（approved）作品可提交「修改申请」**（不限原作者），同样走审核。管理员审核页右上角标注「创建/修改」；四个「审核xx」按钮在管理员有待审表单时显示**红点**。
- **方案：影子行模式**——实体表加 `apply_type VARCHAR(10) DEFAULT 'create'` + `original_id BIGINT NULL` + `idx_*_original_id`。任意登录用户 PUT approved 原记录时**不原地改**，而是**复制原记录为一行新记录**（新 id、apply_type='update'、original_id=原id、status='pending'、关联表写到影子 id），原记录保持 approved 照常上架显示；管理员审核影子行通过 → `applyEdit` 把影子行**合并回原记录**（主表字段 UPDATE + 关联表迁移：先 `DELETE ... WHERE <fk>=原id` 再 `UPDATE ... SET <fk>=原id WHERE <fk>=影子id`，避免复合主键冲突）→ **删除影子行**；拒绝 → 影子行 rejected + reason（提交者可看）。管理员编辑 approved 原记录**原地改**（不申请）；创建者编辑自己 pending/rejected 原地改；**非创建者不能改他人 pending/rejected（403）**。**moe_awarded(+10 萌点)只在创建申请通过时给，修改申请通过不给**。
- **表**：四实体表 schema.sql 建表 + DatabaseMigrator `ensureColumn`+`ensureIndex` 双写幂等（`apply_type`/`original_id`/索引）。
- **后端**：四 model 各加 `applyType/originalId/originalName`（JSON apply_type/original_id/original_name；originalName = 被改正式记录名称，供审核列表显示「修改自：xxx」，BASE_COLUMNS 加子查询 `(SELECT g2.name FROM <表> g2 WHERE g2.id=<表>.original_id)`）；四 Dao 各加 `createUpdateShadow(originalId, ...)`（复制+关联写影子 id）/`applyEdit(editId)`（@Transactional 合并+迁移+删影子）/`deleteShadows(originalId)`（删原记录前兜底清影子）/`countPending()`；四 Controller `update` 权限三分支（`canEdit = isAdmin || (isCreator && !approved)` 原地改 / **`canApply = !isAdmin(user) && approved` → createUpdateShadow 返 201 `{id,status:'pending',apply_type:'update',original_id,message:'修改申请已提交，等待审核'}`** / else 403）、`review` 按 `apply_type='update'` 走 applyEdit 返回原记录、`delete` 先 deleteShadows。⚠️ **canApply 必须排除 admin（`!isAdmin(user)`）**——canApply 分支优先，否则 admin 编辑 approved 会错误走影子行。`NormalizedGalgameRequest` 从 GalgameController 私有 record **提升为公共顶层 record**（DAO 在 com.galgame.dao 包要用）。新 `controller/ReviewCountController.java`：`GET /api/review/pending-count`（手动 token 鉴权 adminGate）→ `{galgame, company, staff, character, total}`（四 Dao 各 `countPending()`）。
- **前端**：4 个 ReviewView 卡片加 `.gal-review-name-row`（名称 + el-tag：update→`type="warning"`「修改」/否则 `type="primary"`「创建」）+ 来源行 `修改自：{{ original_name || ('#'+original_id) }}`（v-if apply_type==='update'，旧后端/无字段兜底不渲染）；4 个列表页「审核xx」按钮外包 `el-badge :value="reviewCount?.[key] ?? 0" :hidden="!isAdmin || !(reviewCount?.[key]>0)"`，`onMounted` 里 isAdmin 才拉 `/review/pending-count`；4 详情页 `canEdit` 改 **`isAdmin || isCreator || (user.value?.id != null && detail.value?.status === 'approved')`**（任意登录用户对已上架作品可发起修改），保存成功分场景提示（`!isAdmin && detail.status==='approved'` → 「修改申请已提交，等待管理员审核」），**画廊上传/删除、删除作品等写操作改用更严格 `canEditContent = isAdmin || (isCreator && status!=='approved')`** 保持原权限（后端 403 一致）；4 个 MineView 卡片同样标创建/修改 + 来源。theme.css 5.15 段 `.gal-review-name-row`/`.gal-origin-line` 深浅色。
- **冒烟**：`D:\claude code\entity_edit_review_smoke.py` **41/41 PASS**（管理员 = DB 提升 `UPDATE users SET admin_level=1` 后**重新登录**拿 token；断言含创建回归/修改影子 201/原记录隔离/pending 标注+original_name/审核合并+tag·related 迁移/影子 404/拒绝回显/**C（非创建者）对 approved 提交修改 201 + 审核后生效**/**C 改他人 pending 403**/company·staff·character 修改通过/pending-count 200+total=和+非管理员 403/清理清 0）。⚠️ 修改申请测试的 PUT body 需完整（normalizeAndValidate 会校验 categories 合法、tag_ids/related_ids 全部 approved）；**pending 载体不能复用已 approved 的实体**（测「非创建者不能改他人 pending」要新建独立 pending 实体，否则已 approved 的会被误判可改）。
- **部署**：`D:\claude code\deploy_entity_edit_review_to_server.py`。部署时服务器冒烟 **35/35 PASS**、assets 4/4、home/api 200。⚠️ 坑：表迁移断言查 `information_schema.COLUMNS` 五处；pending-count 的 total 断言只验证「=四实体和」（线上可能有真实 pending，不能写死绝对数）。

## 四实体「条目贡献者」+ 修改通过 +10 萌点（已完成本地并部署线上，2026-08-30）
- **定位**：galgame / 会社 / 制作人员 / 角色四实体详情页右下角小字「条目贡献者」，点进 `/xxx/:id/contributors` 单独页列出该条目所有贡献者账号（头像/昵称/@账号/时间，可跳 `/user/:id`）。贡献者 = 创建者 + 所有修改申请审核通过的用户 + **管理员原地直改已上架条目的管理员**。**修改生效也 +10 萌点**（创建申请通过本已 +10；现修改申请审核通过 +10、管理员原地直改已上架 +10）。
- **表**：`entity_contributors(entry_type, entry_id, user_id, created_at, PRIMARY KEY(entry_type,entry_id,user_id), FK user_id→users ON DELETE CASCADE, idx_ec_entry)`。schema.sql + DatabaseMigrator `ensureTableIfAbsent` + `ensureFk` 双写；启动 **backfill**：四实体表存量 `created_by IS NOT NULL` 的行 INSERT IGNORE 回填（entry_type 分别 'galgame'/'company'/'staff'/'character'，created_at=原 created_at）。
- **后端**：`EntityContributor` model（id/username/nickname/avatarUrl/createdAt）；`EntityContributorDao`（`addContribution` INSERT IGNORE 防重复 / `findContributors` JOIN users ORDER BY created_at,user_id / `deleteContributions`）；`EntityContributorController`（`GET /api/entity-contributors?entry_type=&entry_id=`，**公开无需登录**，entry_type 白名单四值否则 400「无效的条目类型。」，缺 entry_id 400「缺少条目 id。」）。四 Controller（Galgame/Company/Staff/Character）注入 `EntityContributorDao` + ENTRY_TYPE 常量：
  - `doCreate`：insert 后 `addContribution`（创建者即贡献者，try-catch 静默）
  - `review` 修改申请通过：**applyEdit 之前** `addContribution(原id, 提交者)` + `claimMoeAward(影子id)` 原子置位成功 → `adjustMoePoints(提交者,10)`，moe_granted 回显（操作者==提交者则 10 否则 0）；返回体统一 map（applied + moe_granted）——Company/Staff/Character 原返回 record，已改 convertValue map（**Company 原本无 ObjectMapper**，补注入 `tools.jackson.databind.json.JsonMapper`；Staff/Character 原本有）
  - `update`：`isAdmin(user) && "approved".equals(原记录status)`（管理员原地直改已上架）→ `addContribution` + `adjustMoePoints(+10)`；创建者改自己 pending/rejected 不触发
  - `delete`：删条目后 `deleteContributions`
  - ⚠️ 修改通过**每次都 +10（无防重）**——与创建申请的单条 moe_awarded 原子防重不同：影子行每次审核通过独立 claimMoeAward 置位；管理员直改已上架每次 +10。
- **前端**：路由 4 条 `/:id/contributors`（`/galgame`、`/company`、`/staff`、`/character`，放各 `/:id` 之后）；新 `EntityContributorsView.vue`（按 **route.name 映射 entry_type**：galgame-contributors→'galgame' 等，entry_id=`route.params.id`；GET `/api/entity-contributors`；loading/error/空态「暂无贡献者」；标题「条目贡献者（N）」；每项头像 `resolveAssetUrl` + 昵称（`nickname||username`）+`@账号` + `formatTime(created_at)`，整卡点击 `router.push('/user/'+id)`；goBack `router.back()` 兜底按类型回列表）；4 详情页信息卡 body 最底部加 `.gal-contributors-link`「条目贡献者」小字（右对齐 12px 灰字 hover 蓝，`goContributors` push 各类型路径，GalgameDetailView L154 / Company L72 / Staff L62 / Character L62）；theme.css 5.16 段 `.gal-contributors-*` light+dark。
- **冒烟**：`D:\claude code\entity_contributors_smoke.py` **28/28 PASS**（管理员 DB 提升；创建即填贡献者/管理员创建即上架 moe+10/B 建 pending 填创建者不加/C 修改通过 moe+10+列贡献者/C 二次通过 moe=20 去重仍 1 条/A 直改 approved +10/A·B 改 pending 不加不列/company·staff·character 修改通过列贡献者/公开 200/非法 type 400/缺 id 400/字段齐全/清理清 0）。⚠️ 登录响应取 id 是 `data["user"]["id"]`（login 返回 `{token, user}`）；清理表名映射 company→**companies**。
- **部署**：`D:\claude code\deploy_entity_contributors_to_server.py`（上传 jar+dist → 重启 8081 → 服务器冒烟 21 项 → 清理 → assets 校验）。部署时服务器冒烟 **21/21 PASS**、assets 5/5、home/api 200。⚠️ 清理 heredoc 的 entity_contributors 残留用「entry_id NOT IN (四个实体表 UNION)」安全删（只删测试条目残留、不碰存量回填）；用户变量用 UA/UB/UC 避开 bash B=URL 坑。

## 制作人员/角色封面图（已完成本地并部署线上，2026-08-30）
- **定位**：staffs / characters 资料页加「封面图」一项并实现封面功能（与 galgame 封面上传同一模式，功能简单 → 本地验证后直接部署）。
- **数据**：`staffs.image`/`characters.image VARCHAR(500) NULL COMMENT '封面图 URL'`（schema.sql 建表 + DatabaseMigrator `ensureColumn` 双写）。
- **后端**：`Staff`/`Character` record 加 `String image`（name/description 后，SNAKE_CASE → JSON 键 `image`）；`StaffDao`/`CharacterDao` **全链路**加 image——BASE_COLUMNS（`s.image`/`ch.image`）、mapRow、`insertWithCharacters`/`insertWithStaffs`、`createUpdateShadow`（影子行也复制 image）、`applyEdit`（UPDATE 合并 image 回原记录）、`updateWithCharacters`/`updateWithStaffs`；`StaffController`/`CharacterController`：`StaffRequest`/`CharacterRequest` + `Normalized*Request` 加 `String image`、`normalizeAndValidate` trim ≤500（超长 400「字段长度超出限制。」）、doCreate/update 透传、新增 `POST /api/staffs/image`/`POST /api/characters/image`（multipart 字段 `file`，detectImageExt 白名单 png/jpg/jpeg/webp/gif + contentType 兜底、≤10MB、存 `uploads/staff_images`/`uploads/character_images`、文件名前缀 `staff_`/`character_`、返回 `{url}`）。⚠️ body 不传 image → 后端 trimToNull 置 null（创建无图条目详情 image 为 null）。
- **前端**：`AddStaffView`/`AddCharacterView` 表单加「封面图片」el-upload（`:show-file-list="false" :http-request="uploadImage" accept="image/*"` → FormData `file` → POST `/staffs/image`|`/characters/image` → `form.image = data.url` + `imagePreview = resolveAssetUrl(data.url)` + `removeCover`，payload 加 `image`）；`StaffDetailView`/`CharacterDetailView` 展示卡 `.gal-detail-cover` 有图显示 img（`v-if="detail.image && !coverError"`，`@error="coverError = true"` 回退占位文字）否则占位，编辑模式加封面上传、`enterEdit` 回填 `form.image`+`imagePreview`（`d.image ? resolveAssetUrl(d.image) : ''`）、`loadDetail` 重置 `coverError`、submit payload 加 `image`；`.add-cover*`（120x150 object-fit cover）`.add-hint` 样式与 AddGalgameView 一致。
- **冒烟**：`D:\claude code\staff_character_cover_smoke.py`（pymysql 直连本地库，管理员 DB 提升）**30/30 PASS**（列就绪/未登录上传401/登录上传200+静态访问200+文件名前缀/非图片400/创建带image详情回显/无image详情null/编辑改image/超长image 400/修改申请影子行复制image+原记录隔离/applyEdit合并image回原记录/清理清0）。⚠️ 两个冒烟坑：① pending 条目详情匿名 GET 返回 404 → 断言 pending 的 image 必须带创建者 token；② 非图片测试 multipart 必须传 `content_type='text/plain'`（脚本默认 image/png 会被后端 contentType 兜底判合法 → got=200 误 FAIL）。
- **部署**：`D:\claude code\deploy_staff_character_cover_to_server.py`（上传 jar+dist → 重启 8081 → 服务器冒烟 28 项 → 清理含磁盘 `rm -f uploads/staff_images/*.png uploads/character_images/*.png` → assets 校验）。部署时服务器冒烟 **28/28 PASS**、assets 5/5、home/api 200。
- **列表页封面（追加，2026-08-30）**：`StaffView`/`CharacterView` 卡片左侧 `.gal-cover` 区改为显示封面——有图 `v-if="s.image && !s._coverError"` 渲染 `<img :src="resolveAssetUrl(s.image)" loading="lazy" @error="coverImgError(s)">`（`coverImgError(item)` 置 `item._coverError=true` 回退占位），无图/加载失败显示「制作人员」/「角色」占位；`.gal-cover img { width:100%;height:100%;object-fit:cover }`。模式与 `GalgameView` 列表卡片一致。仅前端改动，后端列表接口 `GET /api/staffs`、`/api/characters` 已通过 BASE_COLUMNS 返回 `image`。验证：本地 `D:\claude code\quick_list_image_check.py` **3/3 PASS**；部署 `D:\claude code\deploy_list_cover_to_server.py`（**仅传 dist**，后端 jar 未改 → 不重启；服务器冒烟 8 项含列表 image 回显 + 静态访问，⚠️ 冒烟内精确删除本次上传封面文件，勿 `rm` 通配符误删线上真实封面）**8/8 PASS**、assets 5/5。
- **galgame 详情角色栏封面（追加，2026-08-30）**：galgame 详情页「角色」tab 每个角色卡片左侧显示封面小图（有图 `v-if="c.image && !c._coverError"` → `<img :src="resolveAssetUrl(c.image)" loading="lazy" @error="charCoverError(c)">`，无图/加载失败占位「角色」），右侧名称+description；制作人员栏**不加**封面（用户明确）。后端：`model/CharacterBrief.java` record 加 `String image`（id, name, image, description）；`GalgameDao.findCharacterBriefs` SQL 加 `c.image`、mapRow 加 `rs.getString("image")`；⚠️ `StaffDao.findLinkedCharacters`（制作人员详情页的角色列表）也 new CharacterBrief，SQL 顺手加 `c.image` + 构造器补 image（否则编译失败，4 参 record）。验证：本地 `D:\claude code\galgame_char_cover_smoke.py` **11/11 PASS**（详情 characters 数组 image 回显 + description 保留 + staffs 数组不受影响 + 清理清0）；部署 `D:\claude code\deploy_galgame_char_cover_to_server.py`（jar+dist → 重启 8081 → 冒烟 8 项，冒烟内精确删除本次上传封面）**8/8 PASS**、assets 5/5。
- **封面自适应（追加，2026-08-30）**：制作人员/角色详情大封面 `.gal-detail-cover` 由 240×240 固定 + `object-fit:cover`（会裁掉图）改为**宽度自适应**——容器 `width:240px; max-width:100%`（去固定 height），img `width:100%; height:auto` 完整显示不裁切（竖图自然增高、横图变矮）；无图占位 `.gal-detail-cover-placeholder` 固定 `height:240px` 维持正方形。galgame 详情角色小封面 `.gal-char-cover` 由 64×84 + cover（裁切）改为**高度固定 84px + 宽度自适应**——容器只留 `height:84px`（去 width），img `height:100%; width:auto` 按图比例完整显示（不同角色宽度不同、高度对齐）；无图占位固定 `width:64px; height:84px`。纯 CSS 改动。通用纯前端部署脚本 `D:\claude code\deploy_dist_only.py`（仅传 dist，后端未改不重启，assets 逐文件 HTTPS 校验）部署，assets 79/79、home/api 200。⚠️ **flex 拉伸坑**：详情卡片 `.gal-detail-card` 是 `display:flex` 且默认 `align-items:stretch`，`.gal-detail-cover` 会被拉到与右侧文字同高——图矮文字高时图下方伸出一块空背景。必须给 `.gal-detail-cover` 加 `align-self: flex-start`（StaffDetail/CharacterDetail 两处都要，2026-08-30 修复）。galgame 详情角色小图 `.gal-char-card` 已 `align-items: center`，无此问题。
